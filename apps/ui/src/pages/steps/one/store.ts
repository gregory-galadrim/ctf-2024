import { makeAutoObservable } from 'mobx';

/**
 * StepOneStore
 *
 * The MOBX store of StepOne
 */
export class StepOneStore {
  // Properties

  // Constructor
  constructor() {
    makeAutoObservable(this);
  }

  // Functions
}
